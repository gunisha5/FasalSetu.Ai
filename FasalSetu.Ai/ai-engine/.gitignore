# Python virtual environment
venv/
.venv/
__pycache__/
*.pyc
*.pyo
*.pyd
*.pycache

# Secrets — never commit
.env
secrets/
*.json

# Allow ML models to be deployed
# models/*.pkl
# Allow datasets
!data/*.csv
# Only ignore the raw training features if they are too big, but let's allow them for now
#!data/training_features.csv

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db
